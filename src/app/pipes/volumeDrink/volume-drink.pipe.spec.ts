import { VolumeDrinkPipe } from './volume-drink.pipe';

describe('VolumeDrinkPipe', () => {
  it('create an instance', () => {
    const pipe = new VolumeDrinkPipe();
    expect(pipe).toBeTruthy();
  });
});
